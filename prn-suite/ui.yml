---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: ui-${CI_COMMIT_REF_SLUG}
  name: ui-${CI_COMMIT_REF_SLUG}
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: ui-${CI_COMMIT_REF_SLUG}
  template:
    metadata:
      labels:
        app: ui-${CI_COMMIT_REF_SLUG}
      name: ui-${CI_COMMIT_REF_SLUG}
    spec:
      containers:
        - image: $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_NAME_$CI_PIPELINE_ID
          name: ui-${CI_COMMIT_REF_SLUG}
          env:
            - name: NODE_ENV
              value: ${NODE_ENV}
          readinessProbe:
            periodSeconds: 15
            timeoutSeconds: 2
            successThreshold: 2
            failureThreshold: 2
            httpGet:
              path: /
              port: 3002
          livenessProbe:
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 2
            successThreshold: 1
            failureThreshold: 3
            httpGet:
              path: /
              port: 3002
      restartPolicy: Always
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: ui-${CI_COMMIT_REF_SLUG}
  name: ui-${CI_COMMIT_REF_SLUG}
spec:
  ports:
    - port: 80
      targetPort: 3002
      protocol: TCP
      name: http
  selector:
    app: ui-${CI_COMMIT_REF_SLUG}
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  labels:
    app: ui-${CI_COMMIT_REF_SLUG}
  name: ui-${CI_COMMIT_REF_SLUG}
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
      - ${DOMAIN}
  rules:
    - host: ${DOMAIN}
      http:
        paths:
          - backend:
              serviceName: ui-${CI_COMMIT_REF_SLUG}
              servicePort: 80
            path: /
